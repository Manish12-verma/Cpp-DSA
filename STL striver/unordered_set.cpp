void explainUset(){
    unordered_set<int>st;
    //lower bound and upper boun functions
    //does not works,rest all the functions are same 
    //as above,it doesn't stores in any particular order it has a better 
    //complexity than set in most cases,except some when collision happens 
    

}