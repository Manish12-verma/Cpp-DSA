void explainList(){
    list<int>ls;


    ls.push_back(2);
    ls.emplace_back(4); // it insert the element at the back of the array.

    ls.push_front(5);
    ls.emplace_front(); {2,4}

    //rest functions are same as the vector
    
}